package net.engineeringdisgest.journalApp.schedular;

import net.engineeringdisgest.journalApp.cache.AppCache;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.scheduling.annotation.Scheduled;

public class CacheSchedular {

//    @Autowired
//    private AppCache appCache;
//
//    @Scheduled(cron = "0 0/10 * ? * *") // every 10 mins it will reload the appCache, we have cron to do this
//    public void clearAppCache() {
//        appCache.init();
//    }
}
